import 'package:test/test.dart';

void main() {
  group("visa", () {
    group("CountryCategory", () {
      test("", () {});
    });
    group("CountryCategoryList", () {});
    group("CountryVisaInfo", () {});
    group("VisaData", () {});
    group("Destinations", () {});
    group("Destination", () {});

    group("VisaInfo", () {});

    group("Error", () {});
  });
}
